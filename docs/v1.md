# Production Readiness Evidence
Date: 2025-10-24

This document captures evidence for configuration handling, secrets scanning, health checks, startup guards, and coverage gates across the repository.

## Config Externalization & Secrets
- Env loader centralization: `packages/shared/src/env.ts` loads `.env` (package CWD) and optionally repo root, and exposes `env.*` keys.
  - Keys include `DATABASE_URL`, `REDIS_URL`, `MINIO_*`, `OPENAI_API_KEY`, `LANGFUSE_*`, `OTEL_EXPORTER_OTLP_ENDPOINT`.
  - Secrets are sourced from `process.env` with dev defaults for local use (e.g., MinIO admin creds). In CI, secrets are injected via the platform.
  - Evidence: `packages/shared/src/env.ts` lines 1–27.
- MinIO client uses env-sourced credentials: `packages/shared/src/minioClient.ts` constructs the client from `env.MINIO_ENDPOINT`, `env.MINIO_ACCESS_KEY`, `env.MINIO_SECRET_KEY`; bucket name is non-secret.
  - Evidence: `packages/shared/src/minioClient.ts` lines 1–29.
- OpenAI client usage pulls API key from env in services (no hardcoded keys):
  - `packages/planner/src/server.ts` → `new OpenAI({ apiKey: env.OPENAI_API_KEY })`
  - `packages/implementer/src/server.ts` → `new OpenAI({ apiKey: env.OPENAI_API_KEY })`
  - `packages/validator/src/server.ts` → uses `process.env.OPENAI_API_KEY` in judge path.
- Secrets scan (Gitleaks):
  - Command: `npm run compliance:secrets`
  - Result: no leaks found.
  - Evidence artifact: `.automation/evidence/compliance/gitleaks.sarif` (generated).

Status: Config externalization PASS; Secrets scan PASS.
## Health Checks & Startup Guards
- Health endpoints present:
  - Runner: `packages/runner/src/server.ts` exposes `GET /healthz` returning `{ ok: true }`.
  - Validator: `packages/validator/src/server.ts` exposes `GET /healthz` returning `{ ok: true }`.
- Health endpoints missing:
  - Gateway: `packages/gateway/src/server.ts` does not define `/healthz`.
  - Planner: `packages/planner/src/server.ts` does not define `/healthz`.
  - MCA: `packages/mca/src/server.ts` does not define `/healthz`.
- Startup guards:
  - MCA initializes LangGraph `PostgresSaver` and attempts `setup()`; on failure (non-test), logs error and exits. This guards against running without checkpoint storage.
    - Evidence: `packages/mca/src/server.ts` — `PostgresSaver` setup with exit on error.
  - Services call `startOtel(<service>)` at boot, which is best-effort telemetry init; failures here do not break startup.

Status: Health checks PARTIAL (runner/validator pass; gateway/planner/mca missing). Startup guards PARTIAL (MCA guarded; others minimal).
## Coverage Gates
- Ran unit/integration tests with coverage:
  - Command: `npm test -- --coverage`
  - Coverage summary: `coverage/coverage-summary.json`
  - Lines: 70.78% (global)
  - Branches: 63.01%
- Enforced global threshold with repository script:
  - Command: `npm run compliance:coverage`
  - Result: FAIL — `Coverage (statements) 70.78% is below threshold 80%.`
  - Evidence artifact: `coverage/coverage-summary.json` and `coverage/coverage-final.json`.
- Validator package specific gate (reference): requires ≥90% lines; current run did not compute per-package validator coverage in the global pass. Use `npm run test:validator` then `npm run compliance:validator-coverage` for validator-only enforcement.

Status: Global coverage threshold FAIL (70.78% < 80%). Validator-specific coverage not measured in this pass.

## Summary Status
- Config externalization: PASS
- Secrets scanning: PASS
- Health checks: PARTIAL
- Startup guards: PARTIAL
- Coverage gate (global): FAIL

## Next Actions (Recommended)
- Add `/healthz` to Gateway, Planner, and MCA services.
- Raise global coverage ≥ 80%; focus on low-covered files (e.g., `packages/mca/src/server.ts`, `packages/shared/src/index.ts`, `packages/shared/src/vfs.ts`).
- Optionally add `compliance:secrets` to CI workflow and publish SARIF to code scanning for visibility.
